// Инициализация массива
let numbersToTen: number[] = [];

// Цикл для заполнения массива числами от 0 до 10
for (let i = 0; i <= 10; i++) {
    numbersToTen.push(i); // Добавляем текущее значение i в массив
}

// Выводим массив в консоль
console.log(numbersToTen);
