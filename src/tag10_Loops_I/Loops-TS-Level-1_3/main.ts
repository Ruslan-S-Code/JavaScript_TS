// Инициализация переменной-счетчика
let counter: number = 0;

// Цикл while с условием
while (counter < 10) {
    console.log(`Hallo World + ${counter}`); // Вывод строки с текущим значением counter
    counter++; // Увеличиваем значение counter на 1
}
