// app.ts

// Импортируем функцию addFive из файла calculator.ts
import { addFive } from "./calculator";

// Вызываем функцию addFive трижды с разными числами и выводим результаты в консоль
console.log(addFive(10)); // 15
console.log(addFive(25)); // 30
console.log(addFive(50)); // 55
